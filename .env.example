DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True

# Database (SQLite default used when DATABASE_URL is not configured)
DATABASE_URL=sqlite:///db.sqlite3

# Email / SMTP
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USE_TLS=True
SMTP_USER=info@click-home.co.il
SMTP_PASS=change-me
DEFAULT_FROM_EMAIL=Click Home <info@click-home.co.il>
ADMIN_EMAIL=itzik@click-home.co.il

# SSQ admin access
ADMIN_PASSWORD=change-me

# Secure supplier upload storage (private path recommended)
STORAGE_PATH=C:/click_home_system/private_uploads/ssq
